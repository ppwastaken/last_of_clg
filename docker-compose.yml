services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: iac
      POSTGRES_PASSWORD: iac
      POSTGRES_DB: iac
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
